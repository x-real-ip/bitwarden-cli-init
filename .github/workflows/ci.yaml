---
name: Continuous integration

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  yamllint:
    name: Yaml lint
    uses: x-real-ip/github-actions/.github/workflows/yamllint.yaml@v3

  build-and-push-image:
    name: Build and Push Image
    needs: [yamllint]
    uses: x-real-ip/github-actions/.github/workflows/build-and-push.yaml@v3
    with:
      push: true
